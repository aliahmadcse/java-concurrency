package codes.aliahmad;

public class VolatileKeyword
{
  private volatile boolean flag = true;
  private volatile int count = 0;

  public static void main(String[] args)
  {
//    volatile keyword
//    volatile keyword is used to make sure that the value of the variable is always read from the main
//    memory and not from the processor cache
//    this way, it creates visibility for each thread and they see the changes made by each thread

  }
}
